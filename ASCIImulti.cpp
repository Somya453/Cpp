#include <bits/stdc++.h> // header file includes every Standard library
using namespace std;

int main() {
	// Your code here
    char a, b;
    cout << "Enter two characters: ";
    cin>>a>>b;

    int(ch); // Convert characters to integers
    //if we want to multiply characters, we can convert them to their ASCII values
    //**if we want to convert the intereger into characters, we will use "char(int)"  */

    int result=a*b;
    cout<<result;

    return 0;
}